// Pure client-rendered SPA: no server-side rendering, no prerendering.
// All routing happens in the browser; the backend just serves the static shell.
export const ssr = false;
export const prerender = false;
